<Application x:Class="FileEncryptorWpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="Views/GeneralWindow.xaml">
    <Application.Resources>
        <!-- Style the error validation by showing the text message under the field -->
        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid HorizontalAlignment="Stretch">
                            <DockPanel HorizontalAlignment="Right">
                                <Grid HorizontalAlignment="Stretch">
                                    <AdornedElementPlaceholder x:Name="errorControl"/>
                                </Grid>
                                <Image Width="16" Height="16" Margin="-26,0,0,0"  ToolTip="{Binding [0].ErrorContent}" Source="C:\Users\luka.budrak\Desktop\cryptor-wpf-project\delete-icon.png"></Image>
                                
                            </DockPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>